var rat = {
    setScore: function (score) {
        if (score%1!==0) {
            return score;
        } else {
            return score + ".0"
        }
    }
}

module.exports = {//wxs是单例模式
    setScore: rat.setScore
}